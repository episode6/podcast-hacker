<?xml version="1.0" encoding="utf-8"?>
<!-- debug builds are yellow regardless of snapshot-ness: this overrides the dark
     snapshot background too, since local/PR debug builds are almost always snapshot
     versions and would otherwise never show the debug color -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#FFD600"
        android:pathData="M0,0h108v108h-108z" />
</vector>
