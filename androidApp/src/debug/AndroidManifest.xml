<?xml version="1.0" encoding="utf-8"?>
<!-- debug-only: permit cleartext to localhost so device verification can serve fixture
     feeds over `adb reverse` (physical devices enforce the no-cleartext default; some
     emulator images don't) -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:networkSecurityConfig="@xml/network_security_config_debug" />
</manifest>
