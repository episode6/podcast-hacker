package com.episode6.podcasthacker

/**
 * Per-platform context required to build the app's dependency graph. On Android this is an
 * `android.content.Context`; on desktop and iOS it's an empty placeholder.
 */
expect class PlatformContext
